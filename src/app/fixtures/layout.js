export const metadata = {
  title: 'Fixtures',
  description: 'ThePitchBasket Fixtures',
}

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
