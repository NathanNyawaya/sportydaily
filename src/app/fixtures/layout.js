export const metadata = {
  title: 'Fxtures',
  description: 'ThePitchBasket Fixtures',
}

export default function RootLayout({ children }) {
 return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
